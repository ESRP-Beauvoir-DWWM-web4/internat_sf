{# {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_row(form.dateEntree) }}
    {{ form_row(form.section) }}
    {{ form_row(form.nom) }}
    {{ form_row(form.prenom) }}
    {{ form_row(form.handicap) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }} #}








{{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}
    <div class="text-center w-50 mx-auto">
                <div class="d-flex justify-content-between mb-1">
                {{ form_label(form.dateEntree, null, {'label': 'Date entrée:'}) }}
                {{ form_widget(form.dateEntree, {'attr': {'class': 'form-control w-75'}}) }}
                </div>
                <div class="d-flex justify-content-between mb-1">
                {{ form_label(form.section, null, {'label': 'Section :'}) }}
                {{ form_widget(form.section, {'attr': {'class': 'form-control w-75'}}) }}
                </div>
                <div class="d-flex justify-content-between mb-1">
                {{ form_label(form.nom) }}
                {{ form_widget(form.nom, {'attr': {'class': 'form-control w-75'}}) }}          
                </div>
                <div class="d-flex justify-content-between mb-1">
                {{ form_label(form.prenom) }}
                {{ form_widget(form.prenom, {'attr': {'class': 'form-control w-75'}}) }}
                </div>
                <div class="d-flex justify-content-between mb-1">
                {{ form_label(form.handicap) }}
                {{ form_widget(form.handicap, {'attr': {'class': 'form-control w-75'}}) }}
                </div>
                <button class="btn btn-custom btn-primary">{{ button_label|default('Save') }}</button>
            
        
        </div>
{{ form_end(form) }}



