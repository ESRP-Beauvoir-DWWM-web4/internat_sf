{% extends 'base.html.twig' %}

{% block title %}Edit Chambre occupant{% endblock %}

{% block body %}
<div>
        <h1 class="text-center">Mettre à jour l'occupant</h1>

        <div  class="d-flex flex-column">

                {{ include('chambre/_form_occupant.html.twig', {'button_label': 'Mettre à jour'}) }}

            <div class="d-flex flex-column align-items-center">

                <a href="{{ path('app_chambre_index') }}" class="btn btn-custom btn-secondary">Retour à la liste</a>

                {{ include('chambre/_delete_form.html.twig') }}
            </div>
        </div>
</div>
{% endblock %}
