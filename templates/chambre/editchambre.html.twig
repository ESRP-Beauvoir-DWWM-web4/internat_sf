{% extends 'base.html.twig' %}

{% block title %}Edit Chambre{% endblock %}

{% block body %}
    <div>
            <h1 class="text-center">Modifier la Chambre</h1>

            <div  class="d-flex flex-column">
                
                    {{ include('chambre/_form_chambre.html.twig', {'button_label': 'mettre à jour'}) }}
                
                <div class="d-flex flex-column align-items-center">
                    <a href="{{ path('app_chambre_index') }}" class="btn btn-custom btn-secondary">Retour à la liste</a>
                
                
                    {{ include('chambre/_delete_form.html.twig') }}
                </div>
                
            </div>
    </div>
{% endblock %}
