<form method="post" action="{{ path('app_etatlieux_delete', {'id': etatlieux.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etatlieux.id) }}">
    <button class="btn bg-danger m-3">Supprimer</button>
</form>

